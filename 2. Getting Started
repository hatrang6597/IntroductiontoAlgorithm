'''Insertion sort
Input: A sequence of n numbers (a1, a2,..., an) 
Out put: A permutation (reordering) of the input sequence (a'1, a'2,..., a'n) such that increase (a'1 < a'2 < ...< a'n) '''

for j in range(2, len(A)): 
    key = A(j) 
    i = j - 1
    while i > 0 or A[i] > key:
        A[i + 1] = A[i]
        i = i - 1 
    A[i+1] = key
    
